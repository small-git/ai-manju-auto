---
description: 完整漫剧生成流水线规则（AutoDL API）
alwaysApply: true
---

# 漫剧主链

- 默认分辨率固定为 `768p横`（竖屏任务才用 `768p竖`）。
- **一故事一剧本**：每个 `story_id` 是独立宇宙；成片前必须 `--story <story_id>` 显式引用，禁止跨故事混跑导致剧情串戏。
- 上游账本：`stories/<story_id>/story.json`（剧本 → 人物 → 环境 → 道具 → 分镜）。
- 成片主链：多图参考 H3（`manhua_video_ref` / `manhua_video_ref_hq`），不要用纯文生 `manhua_video_t2v` 做人设主生成。
- 镜间连续用 `manhua_bridge`（首尾帧）；对白用 `manhua_lipsync`。
- 定妆/静帧可用 GPT/Gemini、模型广场生图 API，或外部面板；产物需公网 URL 写入 `ref_images` / `still_url`。
- 全链路注入同一画风锁 + 身份锁；**单镜只改 `action` 与运镜，不改 `identity_lock` / `clue_lock`**。
- 成片前过齐套门闸（定妆批准、道具 sheet、静帧/环境 URL）；计划预审选 `video_ref`/`bridge`/`grid`/`lipsync`。
- 成功后立即下载 results URL（短时有效），落盘到 `runs/<story_id>/<chapter_id>/`（版本化 `03_video/<shot_id>/vNN.mp4`）。
- 工作台入口：`dsh-manhua/workbench.cmd`（P0–P3：道具/门闸/计划/版本/九宫格/TTS/ZIP/画布）。

# StoryPack 优先级

1. `story_id` + `script`（故事边界与真相）
2. `characters[].identity_lock`（跨镜冻结）
3. `props[].clue_lock`（道具/线索冻结）
4. `environments[].scene_card`（场景冻结）
5. `shots[]`（只写动作/运镜/情绪/state/plan_path）

# 禁止

- 无 `--story` 引用就跑成片。
- 用 A 故事文件生成 B 的 `story_id`。
- 把 API token 写进可提交代码或文档示例。
- 二次元转真人、破坏画风的洗图步骤进主链。
- 空字符串占位未使用的 ref 槽。
- 在分镜里改写人物五官/发型/服装身份句。
